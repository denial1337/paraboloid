/*for (int i = 0; i < m*m*m; ++i)
		delete[] t[i];
	delete[] t;
	for (int i = 0; i < mtsize; ++i)
		delete[] mt[i];
	delete[] mt;
	for (int i = 0; i < 3; ++i)
		delete[] params[i];
	delete[] data;
	delete roots;
	for (int i = 0; i < filesize; ++i)
		delete[] data[i];
	delete[] data;*/